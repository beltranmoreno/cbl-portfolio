import { ReactNode } from 'react'

export const metadata = {
  title: 'Sanity Studio - Carmen Ballvé Portfolio',
}

export default function StudioLayout({ children }: { children: ReactNode }) {
  return children
}
